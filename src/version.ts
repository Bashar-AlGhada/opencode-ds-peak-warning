// Release version, shown in the panel footer so a running install can be
// told apart from a stale cached copy (e.g. after `npm install` no-ops on an
// already-configured plugin name). Keep in sync with package.json.
export const PLUGIN_VERSION = "1.3.1"
